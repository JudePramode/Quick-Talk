<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Allows access to files in the app's cache directory -->
    <cache-path
        name="cache"
        path="/" />

    <!-- Allows access to files in the app's files directory -->
    <files-path
        name="files"
        path="/" />

    <!-- Allows access to files in the external storage directory -->
    <external-path
        name="external"
        path="." />

        <external-path
            name="images"
            path="." />



</paths>
